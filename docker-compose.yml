services:
  # 独立部署的邮件发送微服务
  email-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: email-message-service
    restart: on-failure
    volumes:
      - ./config.yaml:/app/config.yaml
    networks:
      - go-net

networks:
  go-net:
    name: go-net
    external: true
